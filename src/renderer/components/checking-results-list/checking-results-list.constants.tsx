// TODO: Eventually this can and probably should go back into checking-results-list.component.tsx.
// But for now, since that references the test checks, that would cause a circular dependency.
const TAB_TYPE_CHECKING_RESULTS_LIST = 'checking-results-list';
export default TAB_TYPE_CHECKING_RESULTS_LIST;
