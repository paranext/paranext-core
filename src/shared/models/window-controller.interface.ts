import { Dispose } from 'platform-bible-utils';

export const WINDOW_CONTROLLER_OBJECT_TYPE: string = 'windowController';

interface IWindowController extends Dispose {}

export default IWindowController;
