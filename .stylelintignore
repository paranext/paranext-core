# Please keep this file in sync with .eslintignore, .prettierignore and .prettierignorerun

# Logs
logs
*.log

# Runtime data
pids
*.pid
*.seed

# Coverage directory used by tools like istanbul
coverage
.eslintcache

# Dependency directory
# https://www.npmjs.org/doc/misc/npm-faq.html#should-i-check-my-node_modules-folder-into-git
node_modules

# OSX
.DS_Store

release/app/buildInfo.json
release/app/dist
release/build
.erb/dll
dev-appdata

.idea
npm-debug.log.*
*.css.d.ts
*.sass.d.ts
*.scss.d.ts

# Packager output
dist

# eslint ignores hidden directories by default:
# https://github.com/eslint/eslint/issues/8429
!.erb
!.storybook

# directories that have separate eslint project
extensions
lib/browserslist-config-detect-electron
lib/eslint-plugin-paranext
lib/papi-dts
lib/platform-bible-react
lib/platform-bible-utils

# generated files
package-lock.json
bin
obj
storybook-static

# yalc
.yalc
yalc.lock

# Local dev packages checked out for linking during CI or local development
dev-packages/

# Raw PT9 CSS snapshots — verbatim from Paratext 9 DevTools, kept as the audit
# reference for the post-MVP extraction pipeline. Uses non-conforming class names
# (`.usfm_<marker>`) by design.
data/pt9-css/

# Playwright test output
e2e-tests/test-results
e2e-tests/playwright-report

# Generated/extracted PT9 CSS snapshots: prettier-formatted for consistency, but
# exempt from stylelint — they are machine output, not authored source, and tripping
# authoring rules (block-no-empty, selector-class-pattern on PT9's .usfm_* names, etc.)
# would force edits that break their fidelity to PT9.
data/pt9-css/


# Embedded Claude Code profile (vendored from ai-prompts; exempt from repo format/lint)
.claude
.context
CLAUDE.md
