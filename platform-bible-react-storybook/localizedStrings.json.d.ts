import { LocalizedStringDataContribution } from 'platform-bible-utils';

/**
 * Localized strings for platform-bible-react components.
 *
 * Contains translations in multiple languages (en, es, etc.) as well as metadata mapping keys to
 * fallback Paratext keys for components used in the platform.
 */
declare const localizedStrings: LocalizedStringDataContribution;

export default localizedStrings;
