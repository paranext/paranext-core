/*
 * auto-generated by derive-storybook-mock.ts; do not edit by hand.
 *
 * Command: closeRestoreSession
 * Callback kind: data-retrieval
 *
 * Stories should import these handlers and pass them as Storybook args.
 * UX may tune *story args* (which handler to wire up) but should never edit
 * this file directly — it is regenerated every time the script runs.
 */

type Args = unknown[];

const closeRestoreSessionSample = {
  status: null,
};

export async function defaultCloseRestoreSessionHandler(..._args: Args): Promise<unknown> {
  return closeRestoreSessionSample;
}

export async function emptyCloseRestoreSessionHandler(..._args: Args): Promise<unknown> {
  return {};
}
