// Placeholder the type-aware rule tester lints in place of: `typeAwareRuleTester` cases name this
// file so the fixture program resolves types for their code, which replaces these contents.
export {};
