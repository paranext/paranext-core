import { Meta, Description } from '@storybook/addon-docs/blocks';
import * as CodeEditorStories from './live-code-demo.stories';

<Meta of={CodeEditorStories} name="Docs" />

# Live code editing guide

This Storybook includes live code editing capabilities powered by `storybook-addon-code-editor`. You can edit React TypeScript code in real-time using the Live Code Editor tab in the Storybook Panel and see changes immediately on the canvas.

## How to Use

1. **Navigate to any Live Edit enabled story** in this section
2. **Look for the live code editor** - It will appear as a a tab in the bottom section.
3. **Edit the code** directly in the editor
4. **See changes instantly** on the canvas above

## Examples to try

* [**BasicExample**](?path=/story/demo-live-code-editor--basic-example)
  <Description of={CodeEditorStories.BasicExample} />
* [**InteractiveCounter**](?path=/story/demo-live-code-editor--interactive-counter)
  <Description of={CodeEditorStories.InteractiveCounter} />
* [**Counter**](?path=/story/demo-live-code-editor--counter)
  <Description of={CodeEditorStories.Counter} />
* [**InputWithState**](?path=/story/demo-live-code-editor--input-with-state)
  <Description of={CodeEditorStories.InputWithState} />

## Editing code

Changes are temporary and won't affect the actual codebase.

* Use Visual Studio Code shortcuts
* <kbd>⌘</kbd><kbd>Z</kbd> or <kbd>Ctrl</kbd>+<kbd>Z</kbd> to undo changes
* <kbd>⌘</kbd><kbd>S</kbd> or <kbd>Ctrl</kbd>+<kbd>S</kbd> to format code

### What's available

* ✅ **React Hooks** - useState, useEffect, and other hooks work out of the box (already imported)
  ```tsx
   // Add state
   const [value, setValue] = useState('initial');

   // Add event handlers
   onClick={() => console.log('clicked')}
  ```
* ✅ **Component Library** - Tailwind styling and Shadcn/ui components like Button and Input
  ```TypeScript
  // First, make components available to your Story:
  const sharedImports = {
    '@/components/shadcn-ui/button': { Button },
    '@/components/shadcn-ui/input': { Input },
  } as const;

  makeLiveEditStory(YourStory, {
    code: `/* your code */`,
    availableImports: sharedImports,
  });

  // Then use them in your live code:
  <Button variant="destructive">Click me</Button>
  <Input placeholder="Type here..." />
  ```
* ✅ **Monaco editor** - Visual Studio Code's editor with familiar shortcuts
* ✅ **TypeScript support** - Full syntax highlighting and IntelliSense

## 🚀 Ready to start?

Head to [**BasicExample**](?path=/story/demo-live-code-editor--basic-example) to try your first live edit!
