// Extends the root .remarkrc.mjs so MDX formatting stays consistent across the repo.
// Update root .remarkrc.mjs to change plugin or formatting settings for both locations.
export { default } from '../../.remarkrc.mjs';
