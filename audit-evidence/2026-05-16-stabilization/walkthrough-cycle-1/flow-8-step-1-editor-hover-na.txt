SKIP / N-A MARKER: Flow 8 Step 1 (Editor annotation hover)

Only OHEBGRK (original-language) editor was open during this dispatch.
OHEBGRK has zero marble-word annotations (it is the source-language resource itself,
not an annotated translation project). Query for `.editor-typed-mark-external-marble-word`
inside the OHEBGRK frame returned 0 matches.

Per Flow 8 step 1 expectations (hover annotated word in EDITOR), this requires an
EDITABLE annotated project (e.g., a translation in progress). None was open under the
minimal-pane state (≤4 dock tabs total: Home + ER + OHEBGRK).

Outcome: skipped as not-applicable in minimal-pane state, not a defect.
